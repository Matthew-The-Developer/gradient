import { GradienttextDirective } from './gradienttext.directive';

describe('GradienttextDirective', () => {
  it('should create an instance', () => {
    const directive = new GradienttextDirective();
    expect(directive).toBeTruthy();
  });
});
